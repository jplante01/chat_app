## Getting Started

Run the frontend:
`npm run dev`

Run the local Supabase dev environment:
`npx supabase start`
`npx supabase stop`

Local service roles for testing the db with the supabase client:

// Alice's credentials
email: 'alice@test.com'
password: 'password123'

// Bob's credentials
email: 'bob@test.com'
password: 'password123'

// Charlie's credentials
email: 'charlie@test.com'
password: 'password123'